from app.openai_client import create_embedding, generate_answer


if __name__ == "__main__":
    # emb = create_embedding("Hello!")
    # print("Embedding length: ", len(emb))

    # ans = generate_answer(
    #     question="What can cause an RDS connection timeout?",
    #     contexts=["RDS connection timeout can be caused by security groups or route tables."],
    # )
    # print(ans)
